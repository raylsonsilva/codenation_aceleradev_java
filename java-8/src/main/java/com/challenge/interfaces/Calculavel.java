package com.challenge.interfaces;

import java.math.BigDecimal;

public interface Calculavel
{
    BigDecimal somar(Object fieldsClass);
    BigDecimal subtrair(Object fieldsClass);
    BigDecimal totalizar(Object fieldsClass);
}
